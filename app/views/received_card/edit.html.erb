<% content_for(:template_name, "card") %>

<%= form_with url: received_card_steps_path, method: :put, local: true do %>
  <%= fields_for(:form, @form, builder: Cfa::Styleguide::CfaFormBuilder) do |f| %>
    <div class="form-card__header spacing-below-35">
      <h2 class="form-card__title">
        <%= t("received_card.header") %>
      </h2>
    </div>
    <div class="form-card__content">
      <div class="grid">
        <div class="grid__item">
          <h3><%= t("received_card.it_looks_like_html") %></h3>
          <div class="rounded-border spacing-below-25">
            <%= image_tag("ca-p-ebt.png", alt: t('received_card.alt_text_card')) %>
          </div>
          <p class="with-padding-small text--left">
            <%= t("received_card.if_you_already_got") %>
          </p>
        </div>
      </div>
    </div>
    <div class="form-card__footer">
      <%= fields_for(:form, @form, builder: Cfa::Styleguide::CfaFormBuilder) do |f| %>
        <%= f.cfa_single_tap_button(
                :received_card,
                raw("<i class=\"button__icon emoji emoji--checkmark\"></i> #{t("shared.yes")}"),
                :yes
            ) %>
        <%= f.cfa_single_tap_button(
                :received_card,
                raw("<i class=\"button__icon emoji emoji--crossmark\"></i> #{t("shared.no")}"),
                :no
            ) %>
      <% end %>
    </div>
  <% end %>
<% end %>
